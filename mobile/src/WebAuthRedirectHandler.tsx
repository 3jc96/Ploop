/**
 * On web, handles edge cases for OAuth (e.g. code in URL on non-redirect routes).
 * The main same-tab redirect flow is handled by RedirectScreen when path is /redirect.
 */
export function WebAuthRedirectHandler() {
  return null;
}
